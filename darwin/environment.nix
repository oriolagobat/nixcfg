_: {
    environment = {
        pathsToLink = [
            "/share/zsh" 
            "/Applications"
        ];
        systemPath = [
            "/opt/homebrew/bin"
        ];
        # variables = {  # TODO: Remove this when/if programs.neovim.defaultEditor works as expected
        #     EDITOR = "nvim";
        # };
    };
}